const { expect } = require('chai');
const sinon = require('sinon');

describe('GDAX', function () {
  this.timeout(100);

  describe('# get Funds', () =>{
    it('should get Funds', () =>{

    });
  });
});